# Dev: täta deploys, tag sätts av CI (dev-<sha>)
image:
  repository: ghcr.io/igor88gomes/electricity-price
  tag: dev-b2a885c
  digest: "sha256:f96dec7a8aa1d7f5e5b5cf71e03a1ee9b87e04c559af3e3c36fe373e14275d93"

resources:
  requests: { cpu: 50m, memory: 128Mi }
  # Om man vill ha egna limits i DEV avkommentera nedan:
  # limits:   { cpu: 200m, memory: 256Mi }

# TODO: Slå på när du Prometheus Operator redo i klustret
serviceMonitor:
  enabled: true
  additionalLabels:

ingress:
  enabled: true
  className: nginx
  annotations: 
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
  hosts:
    - host: dev.electricity-price.local
      paths:
        - path: /
          pathType: Prefix
  tls:
    enabled: true          
    secretName: electricity-price-dev-tls 

networkPolicy:
  enabled: true

