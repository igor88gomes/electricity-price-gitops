# Dev: tÃ¤ta deploys, tag sÃ¤tts av CI (dev-<sha>)
image:
  repository: ghcr.io/igor88gomes/electricity-price
  tag: dev-4f6a847
  digest: "sha256:4fea8de0f1275c6ee83fbaf5468b25c0e8f5276aca8b0cf335d47d479fb3a6d8"

resources:
  requests: { cpu: 50m, memory: 128Mi }
  limits:   { cpu: 200m, memory: 256Mi }

ingress:
  enabled: true
  className: nginx
  annotations: 
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
  hosts:
    - host: dev.electricity-price.local
      paths:
        - path: /
          pathType: Prefix
  tls:
    enabled: true          
    secretName: electricity-price-dev-tls 

networkPolicy:
  enabled: true

alerts:
  enabled: false

serviceMonitor:
  enabled: false
