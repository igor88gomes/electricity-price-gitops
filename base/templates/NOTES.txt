1. The application is exposed as a ClusterIP service on port {{ .Values.service.port }}.

2. Readiness check:
   kubectl -n <namespace> exec -it <pod> -- curl -s localhost:{{ .Values.service.targetPort }}{{ .Values.probes.readiness.path }}

3. Metrics (Prometheus):
   Enabled via ServiceMonitor when serviceMonitor.enabled=true.

4. Ingress (if enabled):
   When .Values.ingress.enabled=true, the application is exposed via Ingress.

   Example hosts per environment (may vary depending on values):
     - DEV:     http://dev.electricity-price.local
     - STAGING: http://staging.electricity-price.local
     - PROD:    http://prod.electricity-price.local

   In local clusters (e.g. Minikube), you may need to add corresponding
   entries in /etc/hosts pointing to the cluster IP.