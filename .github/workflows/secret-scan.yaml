name: Secret Scan (gitleaks)

on:
  pull_request:
  push:
    branches:
      - main          # Run Gitleaks on push only to main, otherwise only on pull requests

permissions:
  contents: read
  pull-requests: read         # Required for pull request diffs
  security-events: write      # Required for SARIF upload

jobs:
  gitleaks:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Gitleaks
        # Install Gitleaks binary directly (stable approach)
        run: |
          set -e
          VERSION="8.24.0"
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${VERSION}/gitleaks_${VERSION}_linux_x64.tar.gz" -o gitleaks.tar.gz
          tar -xzf gitleaks.tar.gz
          sudo mv gitleaks /usr/local/bin/

      - name: Run Gitleaks scan
        # Run Gitleaks against the repository and generate SARIF
        run: |
          gitleaks detect \
            --source . \
            --redact \
            -c .gitleaks.toml \
            --report-format sarif \
            --report-path gitleaks-results.sarif

      - name: Upload SARIF to GitHub
        # Upload SARIF so findings appear under "Code scanning alerts"
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: gitleaks-results.sarif